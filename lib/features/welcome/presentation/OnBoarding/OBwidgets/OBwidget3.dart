import 'package:flutter/material.dart';

class OBwidget3 extends StatelessWidget {
  const OBwidget3({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
