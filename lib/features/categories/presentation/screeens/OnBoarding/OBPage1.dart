import 'package:flutter/material.dart';

class OBPage1 extends StatelessWidget {
  const OBPage1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
