import 'package:flutter/material.dart';

class OBwidget2 extends StatelessWidget {
  const OBwidget2({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
